import React from "react";

import style from "./styles/PowerControl.module.scss";

export default function PowerControl() {
  return (
    <div className={style.wrapper}>
      <div>powercontrol</div>
    </div>
  );
}
